{
  ApiResponse rep=getResource(true).type(MediaType.APPLICATION_JSON_TYPE).accept(MediaType.APPLICATION_JSON).get(ApiResponse.class);
  Organization org=new Organization(rep);
  org.setUserOwner(rep);
  return org;
}

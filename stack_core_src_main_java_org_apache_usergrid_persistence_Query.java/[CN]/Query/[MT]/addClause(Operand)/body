{
  filterClauses.add(equals);
  if (rootOperand == null) {
    rootOperand=equals;
    return;
  }
  AndOperand and=new AndOperand();
  and.addChild(rootOperand);
  and.addChild(equals);
  rootOperand=and;
}

{
  if (maxVersion == null) {
    return observable;
  }
  return observable.filter(new Func1<Edge,Boolean>(){
    @Override public Boolean call(    final Edge edge){
      return UUIDComparator.staticCompare(edge.getVersion(),maxVersion) < 1;
    }
  }
);
}

{
  final Map<Id,UUID> markedVersions=nodeSerialization.getMaxVersions(scope,markedEdges);
  return Observable.from(markedEdges).subscribeOn(Schedulers.io()).filter(new EdgeFilter(this.maxVersion,markedVersions));
}

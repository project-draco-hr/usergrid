{
  List<UUID> ids=getIds();
  if ((ids != null) && (metadata != null) && (ids.size() > 0)) {
    UUID lastId=ids.get(ids.size() - 1);
    cursor=StringUtils.toString(MapUtils.getMapMap(metadata,lastId,"cursor"));
  }
}

{
  this.queue=queue;
  this.consistencyFig=consistencyFig;
  Schedulers.newThread().schedulePeriodically(new TimeoutTask<T>(this,consistencyFig),consistencyFig.getTaskLoopTime(),consistencyFig.getTaskLoopTime(),TimeUnit.MILLISECONDS);
}

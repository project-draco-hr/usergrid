{
  if (clz == null)   return "TEXT";
  if (clz == Integer.class || clz == Long.class || clz == Boolean.class) {
    return "INTEGER";
  }
 else   if (clz == Double.class) {
    return "REAL";
  }
 else   if (clz == Byte.class) {
    return "BLOB";
  }
  return "TEXT";
}

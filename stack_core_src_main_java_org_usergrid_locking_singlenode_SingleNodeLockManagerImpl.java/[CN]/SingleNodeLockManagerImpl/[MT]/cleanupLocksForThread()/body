{
  if (localLocks.get() == null) {
    return;
  }
  for (  Entry<String,ReentrantLock> lockEntry : localLocks.get().entrySet()) {
    unlockInternal(lockEntry.getValue(),lockEntry.getKey());
  }
  localLocks.set(null);
}

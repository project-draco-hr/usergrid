{
  return appIdsObservable.flatMap(new Func1<ApplicationScope,Observable<ApplicationEntityGroup<CollectionScope>>>(){
    @Override public Observable<ApplicationEntityGroup<CollectionScope>> call(    final ApplicationScope applicationScope){
      return getAllEntities(applicationScope,bufferSize);
    }
  }
);
}

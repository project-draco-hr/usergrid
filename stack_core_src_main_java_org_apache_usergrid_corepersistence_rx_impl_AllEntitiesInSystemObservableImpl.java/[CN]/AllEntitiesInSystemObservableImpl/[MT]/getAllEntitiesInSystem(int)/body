{
  return getAllEntitiesInSystem(applicationObservable.getAllApplicationIds().map(new Func1<Id,ApplicationScope>(){
    @Override public ApplicationScope call(    Id id){
      return new ApplicationScopeImpl(id);
    }
  }
),bufferSize);
}

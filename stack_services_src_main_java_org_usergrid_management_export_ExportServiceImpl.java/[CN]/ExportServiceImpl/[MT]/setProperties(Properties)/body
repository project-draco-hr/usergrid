{
  String autoExpireAfterString=(String)usergridProperties.getProperty(PUSH_AUTO_EXPIRE_AFTER_PROPNAME);
  if (autoExpireAfterString != null) {
    pushAutoExpireAfter=Long.parseLong(autoExpireAfterString);
  }
}

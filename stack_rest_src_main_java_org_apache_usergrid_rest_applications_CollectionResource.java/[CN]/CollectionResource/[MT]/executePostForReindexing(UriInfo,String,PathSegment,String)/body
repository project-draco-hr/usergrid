{
  addItemToServiceContext(ui,itemName);
  final ReIndexRequestBuilder request=createRequest().withApplicationId(services.getApplicationId()).withCollection(String.valueOf(getServiceParameters().get(0))).withDelay(1,TimeUnit.SECONDS);
  return executeAndCreateResponse(request,callback);
}

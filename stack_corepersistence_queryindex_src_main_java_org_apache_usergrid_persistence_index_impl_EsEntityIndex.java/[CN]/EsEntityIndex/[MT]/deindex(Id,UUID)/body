{
  String indexId=createIndexDocId(entityId,version);
  client.prepareDelete(indexName,getTypeName(),indexId).setRefresh(refresh).execute().actionGet();
  log.debug("Deindexed Entity with index id " + indexId);
}

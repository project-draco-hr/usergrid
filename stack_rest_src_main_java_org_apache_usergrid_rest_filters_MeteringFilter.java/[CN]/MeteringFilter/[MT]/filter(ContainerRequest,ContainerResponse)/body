{
  try {
    response.setContainerResponseWriter(new ContainerResponseWriterAdapter(response.getContainerResponseWriter()));
  }
 catch (  Exception e) {
    logger.error("Unable to capture response",e);
  }
  return response;
}

{
  last=new UUID[iterators.size()];
  for (int i=0; i < last.length; i++) {
    Iterator<UUID> itr=iterators.get(i);
    if (!itr.hasNext()) {
      completed=true;
      break;
    }
    last[i]=itr.next();
  }
  Arrays.sort(last);
  return this;
}

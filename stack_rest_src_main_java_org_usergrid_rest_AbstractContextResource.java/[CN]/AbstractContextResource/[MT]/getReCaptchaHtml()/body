{
  if (!useReCaptcha()) {
    return "";
  }
  ReCaptcha c=ReCaptchaFactory.newReCaptcha(properties.getProperty("usergrid.recaptcha.public"),properties.getProperty("usergrid.recaptcha.private"),false);
  return c.createRecaptchaHtml(null,null);
}

{
  String template_property="usergrid.view" + removeEnd(this.getClass().getName().toLowerCase(),"resource").substring(AbstractContextResource.class.getPackage().getName().length()) + "."+ template.toLowerCase();
  String redirect_url=properties.getProperty(template_property);
  if (isNotBlank(redirect_url)) {
    sendRedirect(redirect_url);
  }
  System.out.println(this.getClass());
  return new Viewable(template,model,this.getClass());
}

{
  int size=iterators.size();
  if (size == 0) {
    return null;
  }
  if (size == 1) {
    ResultIterator itr=iterators.get(0);
    if (!itr.hasNext()) {
      return null;
    }
    return itr.next();
  }
  return merge();
}

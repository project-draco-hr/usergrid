{
  final RowOp op=new RowOp(){
    @Override public void rowOp(    final ScopedRowKey<MapEntryKey> scopedRowKey,    final ColumnListMutation<Boolean> columnListMutation){
      columnListMutation.putColumn(true,value);
    }
  }
;
  writeString(scope,key,value,op);
}

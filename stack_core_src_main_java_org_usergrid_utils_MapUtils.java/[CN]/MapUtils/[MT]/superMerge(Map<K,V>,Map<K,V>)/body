{
  for (  K k : b.keySet()) {
    V v=b.get(k);
    if (!a.containsKey(k)) {
      a.put(k,v);
      return a;
    }
 else {
      V av=a.get(k);
      if (av instanceof Map) {
      }
    }
  }
  return a;
}

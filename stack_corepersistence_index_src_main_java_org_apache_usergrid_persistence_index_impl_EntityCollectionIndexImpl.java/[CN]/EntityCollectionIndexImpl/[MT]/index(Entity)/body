{
  Map<String,Object> entityAsMap=EntityCollectionIndexImpl.entityToMap(entity);
  IndexRequestBuilder irb=client.prepareIndex(index,scope.getName(),entity.getId().toString()).setSource(entityAsMap).setRefresh(refresh);
  IndexResponse ir=irb.execute().actionGet();
}

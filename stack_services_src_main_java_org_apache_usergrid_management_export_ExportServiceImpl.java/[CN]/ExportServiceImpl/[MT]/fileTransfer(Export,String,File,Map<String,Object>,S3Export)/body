{
  try {
    s3Export.copyToS3(ephemeral,config,appFileName);
    if (ephemeral.exists())     ephemeral.delete();
  }
 catch (  Exception e) {
    export.setErrorMessage(e.getMessage());
    export.setState(Export.State.FAILED);
    return;
  }
}

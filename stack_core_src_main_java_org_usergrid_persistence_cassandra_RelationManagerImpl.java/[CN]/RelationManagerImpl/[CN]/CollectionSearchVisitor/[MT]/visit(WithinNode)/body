{
  Results r=em.getGeoIndexManager().proximitySearchCollection(getHeadEntity(),collectionName,node.getPropertyName(),new Point(node.getLattitude(),node.getLongitude()),node.getDistance(),null,count,false,level);
  return r;
}

{
  Results r=em.getGeoIndexManager().proximitySearchConnections(connection.getIndexId(),node.getPropertyName(),new Point(node.getLattitude(),node.getLongitude()),node.getDistance(),null,MAX_LOAD,false,query.getResultsLevel());
  results.push(r);
}

{
  QuerySlice slice=node.getSlice();
  queryProcessor.applyCursorAndSort(slice);
  GeoIterator itr=new GeoIterator(new GeoIterator.ConnectionGeoSearch(em,connection.getIndexId(),new Point(node.getLattitude(),node.getLongitude()),node.getPropertyName(),0,node.getDistance()),query.getLimit(),slice);
  results.push(itr);
}

{
  Results r=em.getGeoIndexManager().proximitySearchCollection(headEntity,collection.getName(),node.getPropertyName(),new Point(node.getLattitude(),node.getLongitude()),node.getDistance(),null,GEO_MAX,false,query.getResultsLevel());
  results.push(r);
}

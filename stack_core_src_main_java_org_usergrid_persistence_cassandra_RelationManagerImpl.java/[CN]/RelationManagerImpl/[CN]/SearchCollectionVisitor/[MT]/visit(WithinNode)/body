{
  QuerySlice slice=node.getSlice();
  queryProcessor.applyCursorAndSort(slice);
  GeoIterator itr=new GeoIterator(new GeoIterator.CollectionGeoSearch(em,headEntity,collection.getName(),new Point(node.getLattitude(),node.getLongitude()),node.getPropertyName(),0,node.getDistance()),query.getLimit(),slice);
  results.push(itr);
}

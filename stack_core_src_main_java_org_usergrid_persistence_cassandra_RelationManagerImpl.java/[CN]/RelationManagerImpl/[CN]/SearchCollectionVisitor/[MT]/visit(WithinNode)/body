{
  Results r=em.getGeoIndexManager().proximitySearchCollection(getHeadEntity(),collection.getName(),node.getPropertyName(),new Point(node.getLattitude(),node.getLongitude()),node.getDistance(),null,query.getLimit(),false,query.getResultsLevel());
  results.push(r);
}

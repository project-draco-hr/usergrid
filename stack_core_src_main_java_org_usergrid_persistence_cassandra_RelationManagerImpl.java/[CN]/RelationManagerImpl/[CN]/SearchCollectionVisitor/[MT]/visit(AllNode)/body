{
  String collectionName=collection.getName();
  List<UUID> ids=cass.getIdList(cass.getApplicationKeyspace(applicationId),key(headEntity.getUuid(),DICTIONARY_COLLECTIONS,collectionName),query.getStartResult(),null,query.getLimit() + 1,query.isReversed(),indexBucketLocator,applicationId,collectionName);
  Results results=Results.fromIdList(ids,collection.getType());
  this.results.push(results);
}

{
  Entity e=getHeadEntity();
  Map<String,CollectionInfo> collections=getDefaultSchema().getCollections(e.getType());
  if (collections == null) {
    return null;
  }
  return collections.keySet();
}

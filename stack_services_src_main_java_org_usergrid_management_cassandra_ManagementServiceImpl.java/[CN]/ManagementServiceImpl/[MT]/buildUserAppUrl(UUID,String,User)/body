{
  ApplicationInfo ai=getApplicationInfo(applicationId);
  OrganizationInfo oi=getOrganizationForApplication(applicationId);
  return String.format(url,oi.getName(),ai.getName(),user.getUuid().toString()) + "?token=" + getActivationTokenForAppUser(applicationId,user.getUuid());
}

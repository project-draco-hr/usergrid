{
  if ((userId == null) || (newPassword == null)) {
    return;
  }
  EntityManager em=emf.getEntityManager(MANAGEMENT_APPLICATION_ID);
  em.addToDictionary(new SimpleEntityRef(User.ENTITY_TYPE,userId),DICTIONARY_CREDENTIALS,"password",passwordCredentials(newPassword));
}

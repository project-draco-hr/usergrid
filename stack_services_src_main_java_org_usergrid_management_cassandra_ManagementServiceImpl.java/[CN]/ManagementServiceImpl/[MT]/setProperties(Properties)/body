{
  this.properties=properties;
  if (properties != null) {
    maxTokenAge=Long.parseLong(properties.getProperty(PROPERTIES_AUTH_TOKEN_MAX_AGE,"" + MAX_TOKEN_AGE));
    maxTokenAge=maxTokenAge > 0 ? maxTokenAge : MAX_TOKEN_AGE;
  }
}

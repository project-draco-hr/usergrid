{
  if ((userId == null) || (password == null)) {
    return false;
  }
  if (checkPassword(password,getCredentials(MANAGEMENT_APPLICATION_ID,userId,USER_PASSWORD))) {
    return true;
  }
  logger.info("password compare fail for uuid {}",userId);
  return false;
}

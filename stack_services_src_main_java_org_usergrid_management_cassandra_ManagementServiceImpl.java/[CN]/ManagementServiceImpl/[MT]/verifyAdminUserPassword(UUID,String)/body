{
  if ((userId == null) || (password == null)) {
    return false;
  }
  if (checkPassword(password,readUserPasswordCredentials(MANAGEMENT_APPLICATION_ID,userId))) {
    return true;
  }
  logger.info("password compare fail for uuid {}",userId);
  return false;
}

{
  if ((userId == null) || (password == null)) {
    return false;
  }
  User user=emf.getEntityManager(MANAGEMENT_APPLICATION_ID).get(userId,User.class);
  return maybeSaltPassword(MANAGEMENT_APPLICATION_ID,user,password).compare(readUserPasswordCredentials(MANAGEMENT_APPLICATION_ID,userId));
}

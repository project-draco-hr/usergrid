{
  EntityManager em=emf.getEntityManager(applicationId != null ? applicationId : MANAGEMENT_APPLICATION_ID);
  Entity entity=em.get(getEntityRefFromAccessToken(applicationId,token,null,maxTokenAge,null,true));
  return entity;
}

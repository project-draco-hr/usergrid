{
  EntityRef userRef=null;
  try {
    userRef=getEntityRefFromAccessToken(MANAGEMENT_APPLICATION_ID,token,ADMIN_USER,0,"resetpw",true);
  }
 catch (  Exception e) {
    logger.error("Unable to verify token",e);
  }
  return (userRef != null) && userId.equals(userRef.getUuid());
}

{
  EntityRef organizationRef=getEntityRefFromAccessToken(MANAGEMENT_APPLICATION_ID,token,ORGANIZATION,0,"activate",true);
  return (organizationRef != null) && organizationId.equals(organizationRef.getUuid());
}

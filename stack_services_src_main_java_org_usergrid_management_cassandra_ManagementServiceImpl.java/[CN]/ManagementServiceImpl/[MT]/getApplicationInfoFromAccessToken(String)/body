{
  if (!AuthPrincipalType.APPLICATION.equals(AuthPrincipalType.getFromAccessToken(token))) {
    return null;
  }
  Entity entity=geEntityFromAccessToken(MANAGEMENT_APPLICATION_ID,token);
  return new ApplicationInfo(entity.getProperties());
}

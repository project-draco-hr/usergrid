{
  EntityManager em=emf.getEntityManager(applicationId);
  User user=em.get(userId,User.class);
  if (user == null) {
    return;
  }
  if (user.getEmail() == null) {
    return;
  }
  String pin=getCredentialsSecret((CredentialsInfo)em.getDictionaryElementValue(user,DICTIONARY_CREDENTIALS,"pin"));
  sendHtmlMail(properties,user.getDisplayEmailAddress(),"Usergrid Mailer <mailer@usergrid.com>","Your app pin",emailMsg(hashMap("pin",pin),EMAIL_USER_PIN_REQUEST));
}

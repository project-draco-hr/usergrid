{
  if ((userId == null) || (newPassword == null)) {
    return;
  }
  EntityManager em=emf.getEntityManager(applicationId);
  em.addToDictionary(new SimpleEntityRef(User.ENTITY_TYPE,userId),DICTIONARY_CREDENTIALS,USER_PASSWORD,passwordCredentials(newPassword));
}

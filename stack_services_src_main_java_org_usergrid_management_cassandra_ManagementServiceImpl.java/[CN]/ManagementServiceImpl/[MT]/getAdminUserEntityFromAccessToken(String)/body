{
  EntityManager em=emf.getEntityManager(MANAGEMENT_APPLICATION_ID);
  Entity user=em.get(getEntityRefFromAccessToken(MANAGEMENT_APPLICATION_ID,token,ADMIN_USER,MAX_TOKEN_AGE,null,true));
  return user;
}

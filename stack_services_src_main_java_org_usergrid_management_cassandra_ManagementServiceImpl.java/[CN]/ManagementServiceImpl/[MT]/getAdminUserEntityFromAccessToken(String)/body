{
  EntityManager em=emf.getEntityManager(MANAGEMENT_APPLICATION_ID);
  Entity user=em.get(getEntityRefFromAccessToken(MANAGEMENT_APPLICATION_ID,token,ADMIN_USER,maxTokenAge,null,true));
  return user;
}

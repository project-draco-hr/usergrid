{
  entityRef.setUUID(uuid);
  try {
    return (collectionName != null) ? entityManager.searchCollection(entityRef,collectionName,query) : entityManager.searchConnectedEntities(entityRef,query);
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
}

def remove(self):
    if (not self.can_mutate_or_load()):
        raise ValueError('Unable to delete entity: No uuid nor name')
    return Usergrid.DELETE(collection=self.entity_data.get('type'), uuid_name=self.entity_id())

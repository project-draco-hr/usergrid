{
  filterClauses.add(clause);
  if (rootOperand == null) {
    rootOperand=clause;
    return;
  }
  AndOperand and=new AndOperand();
  and.addChild(rootOperand);
  and.addChild(clause);
  rootOperand=and;
}

{
  while (true) {
    if (dataStaxCluster.getCluster().getMetadata().checkSchemaAgreement()) {
      return;
    }
    try {
      Thread.sleep(100);
    }
 catch (    InterruptedException e) {
    }
  }
}

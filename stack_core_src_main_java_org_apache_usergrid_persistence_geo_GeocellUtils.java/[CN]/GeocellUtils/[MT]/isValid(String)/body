{
  if (cell == null || cell.trim().length() == 0) {
    return false;
  }
  for (  char c : cell.toCharArray()) {
    if (GEOCELL_ALPHABET.indexOf(c) < 0) {
      return false;
    }
  }
  return true;
}

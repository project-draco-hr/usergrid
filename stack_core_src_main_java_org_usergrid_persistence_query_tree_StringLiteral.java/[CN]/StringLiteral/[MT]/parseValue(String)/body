{
  this.value=value;
  if ("*".equals(value)) {
    this.value=null;
    return;
  }
  if (value != null && value.endsWith("*")) {
    value=removeEnd(value.toString(),"*");
    finishValue=value + "\uFFFF";
  }
 else {
    finishValue=value;
  }
}

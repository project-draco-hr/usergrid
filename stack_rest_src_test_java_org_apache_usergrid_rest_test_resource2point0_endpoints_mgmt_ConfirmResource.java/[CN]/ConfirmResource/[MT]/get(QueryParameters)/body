{
  WebResource resource=getResource();
  resource=addParametersToResource(resource,queryParameters);
  String obj=resource.type(MediaType.TEXT_HTML_TYPE).accept(MediaType.TEXT_HTML).get(String.class);
}

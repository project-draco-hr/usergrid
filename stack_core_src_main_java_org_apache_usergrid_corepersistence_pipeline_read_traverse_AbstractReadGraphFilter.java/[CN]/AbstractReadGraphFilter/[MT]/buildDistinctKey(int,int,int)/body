{
  final String DISTINCT_KEY_SEPARATOR=":";
  StringBuilder stringBuilder=new StringBuilder();
  stringBuilder.append(sourceHash).append(DISTINCT_KEY_SEPARATOR).append(targetHash).append(DISTINCT_KEY_SEPARATOR).append(typeHash);
  return stringBuilder.toString();
}

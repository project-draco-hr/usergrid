{
  EntityManager em=null;
  if (emf != null) {
    em=emf.getEntityManager(applicationId);
  }
  return applicationContext.getAutowireCapableBeanFactory().createBean(ServiceManager.class).init(this,em,properties);
}

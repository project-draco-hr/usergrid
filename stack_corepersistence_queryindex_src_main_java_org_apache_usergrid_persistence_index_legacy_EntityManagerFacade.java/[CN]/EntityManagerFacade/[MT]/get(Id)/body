{
  CollectionScope scope=new CollectionScopeImpl(orgScope.getOrganization(),appScope.getOwner(),id.getType());
  EntityCollectionManager ecm=getManager(scope);
  return ecm.load(id).toBlockingObservable().last();
}

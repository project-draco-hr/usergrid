{
  CollectionScope scope=new CollectionScopeImpl(orgScope.getOrganization(),appScope.getOwner(),"dummy");
  EntityCollectionManager ecm=getManager(scope);
  EntityCollectionIndex eci=getIndex(scope);
  eci.refresh();
}

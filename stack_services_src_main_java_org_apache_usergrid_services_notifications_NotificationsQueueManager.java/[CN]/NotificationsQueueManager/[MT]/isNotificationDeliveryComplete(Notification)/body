{
  if (notification.getQueued() == null) {
    return false;
  }
  String queuePath=getJobQueueName(notification);
  return !qm.hasPendingReads(queuePath,null) && !qm.hasOutstandingTransactions(queuePath,null) && !qm.hasMessagesInQueue(queuePath,null);
}

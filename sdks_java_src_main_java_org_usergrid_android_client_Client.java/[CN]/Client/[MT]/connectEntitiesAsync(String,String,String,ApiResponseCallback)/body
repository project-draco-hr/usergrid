{
  (new ClientAsyncTask<ApiResponse>(callback){
    @Override public ApiResponse doTask(){
      return connectEntities(connectingEntityId,connectionType,connectedEntityId);
    }
  }
).execute();
}

{
  String responseString=this.getResource().queryParam("access_token",context.getToken().getAccessToken()).type(MediaType.APPLICATION_JSON).get(String.class);
  logger.info("Response: " + responseString);
  return mapper.readValue(new StringReader(responseString),ManagementResponse.class);
}

{
synchronized (lock) {
    super.before();
    if (isReady()) {
      return;
    }
    if (forkCassandra) {
      startCassandraForked();
    }
 else     if (externalCassandra) {
      startCassandraExternal();
    }
 else {
      startCassandraEmbedded();
    }
  }
}

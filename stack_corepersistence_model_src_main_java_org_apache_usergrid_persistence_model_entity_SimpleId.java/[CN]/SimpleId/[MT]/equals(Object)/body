{
  if (this == o) {
    return true;
  }
  if (!(o instanceof Id)) {
    return false;
  }
  final Id id=(Id)o;
  if (!type.equals(id.getType())) {
    return false;
  }
  if (!uuid.equals(id.getUuid())) {
    return false;
  }
  return true;
}

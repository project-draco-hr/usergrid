{
  (new ClientAsyncTask<Device>(callback){
    @Override public Device doTask(){
      UUID deviceId=new DeviceUuidFactory(context).getDeviceUuid();
      return registerDeviceForPush(deviceId,notifier,token,properties);
    }
  }
).execute();
}

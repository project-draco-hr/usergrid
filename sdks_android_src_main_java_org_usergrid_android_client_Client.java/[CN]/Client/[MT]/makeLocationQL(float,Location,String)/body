{
  ql=(location != null ? "within " + distance + " of "+ location.getLatitude()+ ", "+ location.getLongitude() : "") + (((ql != null) && (ql.trim().length() > 0)) ? " and " + ql : "");
  return ql;
}

{
  name=name.toLowerCase();
  HColumn<String,ByteBuffer> column=cass.getColumn(cass.getSystemKeyspace(),APPLICATIONS_CF,name,PROPERTY_UUID);
  if (column != null) {
    UUID uuid=uuid(column.getValue());
    return uuid;
  }
  return null;
}

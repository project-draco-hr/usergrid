{
  EntityManager em=emf.getEntityManager(CpNamingUtils.MANAGEMENT_APPLICATION_ID);
  User entity=em.get(uuid,User.class);
  if (entity != null) {
    OrgUser user=new OrgUser(entity.getUuid(),entity.getUsername(),entity.getEmail(),entity.getCreated());
    return user;
  }
  return null;
}

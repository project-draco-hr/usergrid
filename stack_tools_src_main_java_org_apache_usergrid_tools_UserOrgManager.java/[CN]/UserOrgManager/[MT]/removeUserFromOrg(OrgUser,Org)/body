{
  managementService.removeAdminUserFromOrganization(user.getId(),org.getId(),true);
  logger.info("Removed user {}:{} from org {}:{}",new Object[]{user.getUsername(),user.getId(),org.getName(),org.getId()});
}

{
  EntityManager em=emf.getEntityManager(CpNamingUtils.MANAGEMENT_APPLICATION_ID);
  Group entity=em.get(uuid,Group.class);
  if (entity != null) {
    Org org=new Org(entity.getUuid(),entity.getPath(),entity.getCreated());
    org.sourceValue=entity;
    return org;
  }
  return null;
}

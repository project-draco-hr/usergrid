{
  cass.init();
  setupSystemKeyspace();
  setupStaticKeyspace();
  ((EntityManagerFactoryImpl)emf).initializeApplication(DEFAULT_APPLICATION_ID,DEFAULT_APPLICATION,null);
  ((EntityManagerFactoryImpl)emf).initializeApplication(MANAGEMENT_APPLICATION_ID,MANAGEMENT_APPLICATION,null);
}

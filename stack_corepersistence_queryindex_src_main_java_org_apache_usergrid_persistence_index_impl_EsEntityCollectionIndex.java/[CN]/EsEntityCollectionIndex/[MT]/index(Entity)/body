{
  Map<String,Object> entityAsMap=EsEntityCollectionIndex.entityToMap(entity);
  entityAsMap.put("created",entity.getVersion().timestamp());
  IndexRequestBuilder irb=client.prepareIndex(index,scope.getName(),createIndexId(entity)).setSource(entityAsMap).setRefresh(refresh);
  irb.execute().actionGet();
}

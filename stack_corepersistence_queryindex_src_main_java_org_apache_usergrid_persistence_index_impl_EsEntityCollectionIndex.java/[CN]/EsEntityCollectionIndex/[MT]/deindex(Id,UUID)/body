{
  String indexId=createIndexId(entityId,version);
  client.prepareDelete(index,scope.getName(),indexId).setRefresh(refresh).execute().actionGet();
  logger.debug("Deindexed Entity with index id " + indexId);
}

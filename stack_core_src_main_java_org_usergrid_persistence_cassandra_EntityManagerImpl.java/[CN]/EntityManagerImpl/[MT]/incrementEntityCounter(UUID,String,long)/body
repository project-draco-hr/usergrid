{
  long timestamp=cass.createTimestamp();
  Mutator<ByteBuffer> m=createMutator(cass.getApplicationKeyspace(applicationId),be);
  if (counterUtils.getIsCounterBatched()) {
    counterUtils.batchIncrementEntityCounter(null,entityId,name,value,timestamp,applicationId);
  }
 else {
    counterUtils.batchIncrementEntityCounter(m,entityId,name,value,timestamp,applicationId);
  }
  batchExecute(m,CassandraService.RETRY_COUNT);
}

{
  String msg="";
  while (e != null) {
    String s=e.getMessage();
    if (msg.length() > 0) {
      msg+="; ";
    }
    msg+=e.getClass().getName();
    if (s != null) {
      msg+=": " + s;
    }
    e=e.getCause();
  }
  return msg;
}

{
  boolean fail=false;
  try {
    ApiResponse groupActivity=client.postGroupActivity(GROUP,null);
    fail=(groupActivity.getError() != null);
  }
 catch (  Exception e) {
    fail=true;
  }
  assertTrue(fail);
}

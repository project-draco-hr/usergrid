{
  boolean exists=false;
  try {
    exists=cluster.describeKeyspace(SYSTEM_KEYSPACE) != null && cluster.describeKeyspace(STATIC_APPLICATION_KEYSPACE) != null;
  }
 catch (  Exception ex) {
    logger.error("could not describe keyspaces",ex);
  }
  return exists;
}

{
  if (db_logger.isInfoEnabled()) {
    db_logger.info("deleteColumn cf=" + columnFamily + " key="+ key+ " name="+ column);
  }
  Mutator<ByteBuffer> m=createMutator(ko,be);
  m.delete(bytebuffer(key),columnFamily.toString(),bytebuffer(column),be);
}

{
  if (logger.isInfoEnabled()) {
    logger.info("deleteRow cf=" + columnFamily + " key="+ key+ " timestamp="+ timestamp);
  }
  createMutator(ko,be).addDeletion(bytebuffer(key),columnFamily.toString(),timestamp).execute();
}

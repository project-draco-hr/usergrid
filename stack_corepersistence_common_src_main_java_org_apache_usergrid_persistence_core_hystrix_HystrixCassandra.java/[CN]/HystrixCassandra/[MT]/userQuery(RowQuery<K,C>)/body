{
  return new HystrixCommand<OperationResult<ColumnList<C>>>(USER_GROUP){
    @Override protected OperationResult<ColumnList<C>> run(){
      try {
        return query.execute();
      }
 catch (      ConnectionException e) {
        throw new RuntimeException(e);
      }
    }
  }
.execute();
}

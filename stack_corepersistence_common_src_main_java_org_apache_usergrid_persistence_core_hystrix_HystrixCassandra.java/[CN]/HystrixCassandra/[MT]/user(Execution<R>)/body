{
  return new HystrixCommand<OperationResult<R>>(USER_GROUP){
    @Override protected OperationResult<R> run(){
      try {
        return execution.execute();
      }
 catch (      ConnectionException e) {
        throw new RuntimeException(e);
      }
    }
  }
.execute();
}

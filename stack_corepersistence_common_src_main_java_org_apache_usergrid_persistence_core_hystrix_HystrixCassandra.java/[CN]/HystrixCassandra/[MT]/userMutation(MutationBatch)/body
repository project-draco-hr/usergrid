{
  return new HystrixCommand<OperationResult<Void>>(USER_GROUP){
    @Override protected OperationResult<Void> run(){
      try {
        return batch.execute();
      }
 catch (      ConnectionException e) {
        throw new RuntimeException(e);
      }
    }
  }
.execute();
}

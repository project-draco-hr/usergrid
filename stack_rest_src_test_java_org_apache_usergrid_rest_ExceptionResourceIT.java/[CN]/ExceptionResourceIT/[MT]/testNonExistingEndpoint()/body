{
  try {
    clientSetup.getRestClient().pathResource(getOrgAppPath("non_existant_delete_endpoint")).delete();
  }
 catch (  NotAllowedException e) {
    assertEquals(405,e.getResponse().getStatus());
  }
}

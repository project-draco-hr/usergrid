{
  OpReply reply=new OpReply(opQuery);
  reply.addDocument(map(entry("$err","unauthorized db:" + opQuery.getDatabaseName() + " lock type:-1 client:"+ ((InetSocketAddress)e.getRemoteAddress()).getAddress().getHostAddress()),entry("code",10057)));
  return reply;
}

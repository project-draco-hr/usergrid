{
  m.addDeletion(bytebuffer(key),cf.toString(),DynamicComposite.toByteBuffer(entry.getUuid(),entry.getType(),entry.getTimestampUuid()),ByteBufferSerializer.get(),getTimestampInMicros(entry.getTimestampUuid()) + 1);
  return m;
}

{
  SearchResults<EntityLocationRef> locations=null;
  GeocellQuery baseQuery=new GeocellQuery().withStartCell(startCell);
  locations=GeocellManager.proximitySearch(center,count,maxDistance,EntityLocationRef.class,baseQuery,gqe,MAX_RESOLUTION);
  return locations;
}

{
  HColumn<ByteBuffer,ByteBuffer> column=createColumn(entry.getColumnName(),entry.getColumnValue(),entry.getTimestampInMicros(),ByteBufferSerializer.get(),ByteBufferSerializer.get());
  m.addInsertion(bytebuffer(key),ENTITY_INDEX.toString(),column);
  return m;
}

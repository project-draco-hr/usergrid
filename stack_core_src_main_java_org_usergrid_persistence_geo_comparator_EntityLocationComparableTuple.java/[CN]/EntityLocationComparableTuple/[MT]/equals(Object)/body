{
  if (this == obj) {
    return true;
  }
  if (obj == null) {
    return false;
  }
  if (getClass() != obj.getClass()) {
    return false;
  }
  @SuppressWarnings("unchecked") EntityLocationComparableTuple<T> other=(EntityLocationComparableTuple<T>)obj;
  if (entity == null) {
    if (other.entity != null) {
      return false;
    }
  }
 else   if (!GeocellUtils.getKey(entity).equals(GeocellUtils.getKey(other.entity))) {
    return false;
  }
  return true;
}

{
  CustomCollection users=context.collection("users");
  Map props=new HashMap();
  props.put("username","Nina");
  JsonNode response=users.create(props);
  JsonNode entity=response.findValues("entities").get(0);
  JsonNode sets=entity.path("metadata").path("sets");
  Assert.assertNotNull(sets.path("roles"));
}

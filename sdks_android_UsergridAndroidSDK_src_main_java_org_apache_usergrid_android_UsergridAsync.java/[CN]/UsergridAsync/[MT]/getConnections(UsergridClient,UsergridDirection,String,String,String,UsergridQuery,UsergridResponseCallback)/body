{
  (new UsergridAsyncTask(responseCallback){
    @Override public UsergridResponse doTask(){
      return client.getConnections(direction,type,uuidOrName,relationship,query);
    }
  }
).execute();
}

{
  final FilterPipeline<FilterResult<Candidate>> newFilter=filterPipeline.withFilter(filterFactory.searchCollectionFilter(ql,collectionName,entityType));
  return new CandidateBuilder(newFilter,filterFactory);
}

{
  testQuery(client,index,type,QueryBuilders.termQuery("name","Morgan Pierce"),1);
  testQuery(client,index,type,QueryBuilders.termQuery("name","Pierce"),0);
  testQuery(client,index,type,QueryBuilders.matchQuery("name_ug_analyzed","Pierce"),2);
  testQuery(client,index,type,QueryBuilders.termQuery("company","Blurrybus"),1);
  testQuery(client,index,type,QueryBuilders.termQuery("gender","female"),433);
  testQuery(client,index,type,QueryBuilders.termQuery("contact.email","nadiabrown@concility.com"),1);
}

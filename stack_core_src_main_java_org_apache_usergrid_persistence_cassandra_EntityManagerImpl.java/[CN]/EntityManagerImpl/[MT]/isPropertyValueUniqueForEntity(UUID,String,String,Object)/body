{
  if (!getDefaultSchema().isPropertyUnique(entityType,propertyName)) {
    return true;
  }
  if (propertyValue == null) {
    return true;
  }
  Set<UUID> ownerEntityIds=getUUIDsForUniqueProperty(new SimpleEntityRef("applicaion",applicationId),entityType,propertyName,propertyValue);
  return ownerEntityIds.size() == 0 || ownerEntityIds.contains(ownerEntityId);
}

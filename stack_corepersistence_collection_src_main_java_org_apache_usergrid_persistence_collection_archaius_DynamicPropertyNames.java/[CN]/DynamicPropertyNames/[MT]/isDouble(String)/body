{
  if (!value.contains(".") || value.endsWith("F") || value.endsWith("f")|| value.endsWith("L")|| value.endsWith("l")) {
    return false;
  }
  try {
    Double.parseDouble(value);
    return true;
  }
 catch (  NumberFormatException e) {
    return false;
  }
}

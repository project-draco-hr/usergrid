{
  if (!(v instanceof RunnerGroup)) {
    return false;
  }
  RunnerGroup other=(RunnerGroup)v;
  return new EqualsBuilder().append(user,other.getUser()).append(commitId,other.getCommitId()).append(moduleId,other.getModuleId()).isEquals();
}

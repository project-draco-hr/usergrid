def log_metrics(self):
    self.logger.info(((((('Re-index start=[%s], ' + 'Re-index end =[%s], ') + 'Full Data Migration start=[%s], ') + 'Full Data Migration end=[%s] ') + 'AppInfo Migration start=[%s], ') + 'AppInfo Migration end=[%s] '), self.metrics['reindex_start'], self.metrics['reindex_end'], self.metrics['full_data_migration_start'], self.metrics['full_data_migration_end'], self.metrics['appinfo_migration_start'], self.metrics['appinfo_migration_end'])

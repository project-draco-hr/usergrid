{
  if (logger.isTraceEnabled()) {
    logger.info("QueueSubscriptionResource.getSubPath");
  }
  return getSubResource(QueueSubscriptionResource.class).init(mq,queuePath,subscriptionPath + "/" + subPath);
}

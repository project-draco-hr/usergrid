{
  if (currentCounter.getCreateTimestamp() + graphFig.getCounterFlushInterval() > timeService.getCurrentTime() && currentCounter.getInvokeCount() < graphFig.getCounterFlushCount()) {
    return;
  }
  flush();
}

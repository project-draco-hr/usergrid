{
  writeLockLock.lock();
  try {
    final boolean queued=flushQueue.offer(currentCounter);
    if (queued) {
      currentCounter=new Counter();
    }
  }
  finally {
    writeLockLock.unlock();
  }
}

def log_metrics(self):
    self.logger.info(((('Re-index start=[%s], ' + 'Re-index end =[%s], ') + 'Entity Migration start=[%s], ') + 'Entity Migration end=[%s] '), self.metrics['reindex_start'], self.metrics['reindex_end'], self.metrics['data_migration_start'], self.metrics['data_migration_end'])

{
  logger.info("Refreshing Created new Index Name [{}]",aliasName);
  final RetryOperation retryOperation=new RetryOperation(){
    @Override public boolean doOp(){
      final String tempId=UUIDGenerator.newTimeUUID().toString();
      esProvider.getClient().prepareIndex(aliasName,VERIFY_TYPE,tempId).setSource(DEFAULT_PAYLOAD).get();
      logger.info("Successfully created new document with docId {} in index {} and type {}",tempId,aliasName,VERIFY_TYPE);
      esProvider.getClient().prepareDeleteByQuery(aliasName).setTypes(VERIFY_TYPE).setQuery(MATCH_ALL_QUERY_BUILDER).get();
      logger.info("Successfully deleted all documents in index {} and type {}",aliasName,VERIFY_TYPE);
      return true;
    }
  }
;
  doInRetry(retryOperation);
}

{
  String indexId=createIndexDocId(entity.getId(),entity.getVersion());
  client.prepareDelete(indexName,typeName,indexId).setRefresh(refresh).execute().actionGet();
  log.debug("Deindexed Entity with index id " + indexId);
}

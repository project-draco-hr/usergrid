{
  ApiResponse response=getResource().queryParam("access_token",context.getToken()).type(MediaType.APPLICATION_JSON_TYPE).accept(MediaType.APPLICATION_JSON).get(ApiResponse.class);
  return new Organization(response);
}

{
  super(String.format("Edit %s's groups",username));
  center();
  setClosable(false);
  setModal(true);
  setHeight("100%");
  setWidth("100%");
  this.username=username;
  groupContainer=createGroupDatasource();
  initLayout();
  initContactList();
  initEditor();
  initSearch();
  initButtons();
}

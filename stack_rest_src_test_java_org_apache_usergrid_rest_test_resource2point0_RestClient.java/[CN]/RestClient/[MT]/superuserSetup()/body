{
  HTTPBasicAuthFilter httpBasicAuthFilter=new HTTPBasicAuthFilter("superuser","superpassword");
  client.addFilter(httpBasicAuthFilter);
  this.getResource().path("system/superuser/setup").accept(MediaType.APPLICATION_JSON).type(MediaType.APPLICATION_JSON).get(JsonNode.class);
  client.removeFilter(httpBasicAuthFilter);
}

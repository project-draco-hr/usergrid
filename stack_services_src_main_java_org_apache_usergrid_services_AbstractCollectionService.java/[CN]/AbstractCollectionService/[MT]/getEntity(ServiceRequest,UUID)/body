{
  if (!isRootService()) {
    return null;
  }
  Entity entity=em.get(new SimpleEntityRef(this.getEntityType(),uuid));
  if (entity != null) {
    entity=importEntity(request,entity);
  }
  return entity;
}

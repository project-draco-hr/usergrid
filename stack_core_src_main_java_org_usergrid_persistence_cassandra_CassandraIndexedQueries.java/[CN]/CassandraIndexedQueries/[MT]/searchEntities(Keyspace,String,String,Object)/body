{
  List<Entity> results=new ArrayList<Entity>();
  doSearchEntityIndex(ko,entityType,null,results,propertyName,propertyValue,Schema.getDefaultSchema().getPropertyNamesAsArray(entityType));
  return results;
}

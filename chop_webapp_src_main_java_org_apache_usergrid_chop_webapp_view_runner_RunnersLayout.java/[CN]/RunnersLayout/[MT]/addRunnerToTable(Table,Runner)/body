{
  State state=runnerService.getState(runner);
  StatsSnapshot stats=runnerService.getStats(runner);
  Object[] cells=new Object[]{runner.getUrl(),state.toString(),stats.getPercentageComplete() + "%",DATE_FORMAT.format(new Date(stats.getStartTime()))};
  table.addItem(cells,runner.getUrl());
}

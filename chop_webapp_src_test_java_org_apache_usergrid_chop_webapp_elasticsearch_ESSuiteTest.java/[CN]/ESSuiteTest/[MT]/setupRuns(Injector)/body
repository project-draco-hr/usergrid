{
  String[] runIds=new String[3];
  runDao=injector.getInstance(RunDao.class);
  BasicRun run=new BasicRun(COMMIT_ID_2,RUNNER_ID_2,1,TEST_NAME);
  runDao.save(run);
  runIds[0]=run.getId();
  run=new BasicRun(COMMIT_ID_2,RUNNER_ID_2,2,TEST_NAME);
  runDao.save(run);
  runIds[1]=run.getId();
  run=new BasicRun(COMMIT_ID_3,RUNNER_ID_1,1,TEST_NAME);
  runDao.save(run);
  runIds[2]=run.getId();
  return runIds;
}

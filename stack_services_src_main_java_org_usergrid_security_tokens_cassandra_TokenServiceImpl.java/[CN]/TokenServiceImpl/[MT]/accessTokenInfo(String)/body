{
  UUID uuid=getUUIDForToken(token);
  if (uuid != null) {
    cassandra.setColumn(cassandra.getSystemKeyspace(),TOKENS_CF,uuid,TOKEN_ACCESSED,System.currentTimeMillis(),(int)(maxPersistenceTokenAge / 1000));
  }
}

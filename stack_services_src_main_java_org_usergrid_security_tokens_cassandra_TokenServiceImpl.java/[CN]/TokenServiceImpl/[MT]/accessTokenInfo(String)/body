{
  UUID uuid=getUUIDForToken(token);
  if (uuid != null) {
    cassandra.setColumn(cassandra.getSystemKeyspace(),TOKENS_CF,uuid,TOKEN_ACCESSED,currentTimeMillis(),(int)(maxPersistenceTokenAge / 1000));
  }
}

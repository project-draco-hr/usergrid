{
  final Timer.Context timer=this.readTimer.time();
  try {
    return queue.getMessages(MAX_TAKE,indexProcessorFig.getIndexQueueVisibilityTimeout(),Math.max(1000,queueFig.getQueueClientSocketTimeout() - 1000),AsyncEvent.class);
  }
  finally {
    timer.stop();
  }
}

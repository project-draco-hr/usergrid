{
  StopWatch timer=new StopWatch();
  timer.start();
  SearchResponse sr=client.prepareSearch(index).setTypes(type).setQuery(qb).setFrom(0).setSize(999).execute().actionGet();
  timer.stop();
  assertEquals(num,sr.getHits().getTotalHits());
  logger.debug("Query1 time {}ms",timer.getTime());
}

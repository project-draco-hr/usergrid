{
  StopWatch timer=new StopWatch();
  timer.start();
  SearchResponse sr=client.prepareSearch(index).setTypes(type).setQuery(qb).setFrom(0).setSize(20).execute().actionGet();
  assertEquals(num,sr.getHits().getTotalHits());
  timer.stop();
  logger.debug("Query time {}ms",timer.getTime());
}

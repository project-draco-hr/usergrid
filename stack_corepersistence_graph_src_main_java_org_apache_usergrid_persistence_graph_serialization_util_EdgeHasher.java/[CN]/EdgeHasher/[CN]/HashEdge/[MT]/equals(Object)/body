{
  if (this == o) {
    return true;
  }
  if (!(o instanceof HashEdge)) {
    return false;
  }
  final HashEdge hashEdge=(HashEdge)o;
  if (!Arrays.equals(hashed,hashEdge.hashed)) {
    return false;
  }
  return true;
}

{
  final String hashString=new StringBuilder(edgeType).append(idForColumn.getType()).toString().toLowerCase();
  final byte[] hashBytes=hashString.getBytes(CHARSET);
  return UUID.nameUUIDFromBytes(hashBytes);
}

from Queue import Empty
import json
import logging
import sys
from multiprocessing import Queue, Process
import traceback
from logging.handlers import RotatingFileHandler
import time
import argparse
from usergrid import UsergridClient, UsergridError
__author__ = 'Jeff West @ ApigeeCorporation'
logger = logging.getLogger('UsergridIterator')
sample_config = {'endpoint': {'api_url': 'https://api.usergrid.com', 'limit': 100, }, 'credentials': {'myOrg': {'client_id': '<<client_id>>', 'client_secret': '<<client_secret>>', }, }, }
config = {}
if (__name__ == '__main__'):
    main()

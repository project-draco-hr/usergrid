{
  if (parameters == null) {
    parameters=new ArrayList<ServiceParameter>();
  }
  if (entityId == null) {
    return parameters;
  }
  ServiceParameter p=new IdParameter(entityId);
  parameters.add(p);
  return parameters;
}

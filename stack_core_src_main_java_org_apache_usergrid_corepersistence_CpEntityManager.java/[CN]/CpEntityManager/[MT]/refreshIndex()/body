{
  emf.refreshIndex();
  IndexScope indexScope=new IndexScopeImpl(appScope.getApplication(),appScope.getApplication(),"dummy");
  EntityIndex ei=managerCache.getEntityIndex(indexScope);
  ei.refresh();
}

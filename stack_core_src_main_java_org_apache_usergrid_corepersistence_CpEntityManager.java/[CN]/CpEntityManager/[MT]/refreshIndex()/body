{
  emf.refreshIndexes();
  IndexScope indexScope=new IndexScopeImpl(appScope.getApplication(),appScope.getApplication(),"application");
  EntityIndex ei=managerCache.getEntityIndex(indexScope);
  ei.refresh();
  logger.debug("Refreshed index for system and application: " + applicationId);
}

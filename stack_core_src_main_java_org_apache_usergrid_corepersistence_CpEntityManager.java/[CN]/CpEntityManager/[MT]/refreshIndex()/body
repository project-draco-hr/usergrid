{
  emf.refreshIndex();
  IndexScope indexScope=new IndexScopeImpl(applicationScope.getApplication(),applicationScope.getApplication(),"dummy");
  EntityIndex ei=managerCache.getEntityIndex(indexScope);
  ei.refresh();
}

{
  final EntityIndex ei=getManagerCache().getEntityIndex(applicationScope);
  final EntityIndexBatch batch=ei.createBatch();
  IndexScope collectionIndexScope=new IndexScopeImpl(collectionEntity.getId(),CpNamingUtils.getCollectionScopeNameFromCollectionName(collName));
  batch.index(collectionIndexScope,memberEntity);
  IndexScope entityAllTypesScope=new IndexScopeImpl(collectionEntity.getId(),CpNamingUtils.ALL_TYPES);
  batch.index(entityAllTypesScope,memberEntity);
  IndexScope appAllTypesScope=new IndexScopeImpl(applicationScope.getApplication(),CpNamingUtils.ALL_TYPES);
  batch.index(appAllTypesScope,memberEntity);
  batch.execute();
}

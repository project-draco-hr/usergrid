{
  Preconditions.checkNotNull(entityRef,"entityRef cannot be null");
  CpRelationManager relationManager=new CpRelationManager(managerCache,filterFactory,indexService,this,entityManagerFig,applicationId,entityRef);
  return relationManager;
}

{
  Preconditions.checkNotNull(entityRef,"entityRef cannot be null");
  CpRelationManager relationManager=new CpRelationManager(managerCache,indexService,collectionService,connectionService,indexProducer,this,entityManagerFig,applicationId,entityRef);
  return relationManager;
}

{
  node.getLeft().visit(this);
  node.getRight().visit(this);
  Results right=results.pop();
  Results left=results.pop();
  if (left != null) {
    if (right != null) {
      left.merge(right);
    }
    results.push(left);
  }
 else {
    results.push(right);
  }
}

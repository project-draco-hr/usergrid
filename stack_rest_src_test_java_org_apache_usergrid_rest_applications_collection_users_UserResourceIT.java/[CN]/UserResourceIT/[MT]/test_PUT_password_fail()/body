{
  boolean fail=false;
  try {
    Entity changeResponse=usersResource.entity("edanuff").put(new ChangePasswordEntity("foo","bar"));
  }
 catch (  Exception e) {
    fail=true;
  }
  assertTrue(fail);
}

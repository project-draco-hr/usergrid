{
  boolean fail=false;
  try {
    ApiResponse changeResponse=client.changePassword("edanuff","foo","bar");
    fail=changeResponse.getError() != null;
  }
 catch (  Exception e) {
    fail=true;
  }
  assertTrue(fail);
}

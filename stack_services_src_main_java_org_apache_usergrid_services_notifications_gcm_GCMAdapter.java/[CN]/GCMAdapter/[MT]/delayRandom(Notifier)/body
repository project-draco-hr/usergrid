{
  boolean wasDelayed=false;
  if (isMock(notifier)) {
    try {
      Thread.sleep(new Random().nextInt(300));
      wasDelayed=true;
    }
 catch (    InterruptedException ie) {
    }
  }
  return wasDelayed;
}

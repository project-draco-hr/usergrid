{
  graphLogs=CacheBuilder.newBuilder().maximumSize(graphFig.getShardCacheSize()).build(new CacheLoader<ShardKey,AtomicLong>(){
    public AtomicLong load(    ShardKey key){
      return new AtomicLong();
    }
  }
);
}

{
  if (id == null) {
    List<ConnectedEntityRef> var=getPairedConnections();
    id=getId(getConnectingEntity(),getConnectedEntity(),var.toArray(new ConnectedEntityRef[var.size()]));
  }
  return id;
}

{
  for (  CoordinatedStack stack : registeredStacks.values()) {
    if (stack.getUser().equals(user) && stack.getCommit().equals(commit) && stack.getModule().equals(module)) {
      return stack;
    }
  }
  return null;
}

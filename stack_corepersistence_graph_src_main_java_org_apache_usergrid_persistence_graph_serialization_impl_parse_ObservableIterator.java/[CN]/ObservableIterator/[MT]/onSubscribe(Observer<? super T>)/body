{
  try {
    Iterator<T> itr=getIterator();
    while (itr.hasNext()) {
      observer.onNext(itr.next());
    }
    observer.onCompleted();
  }
 catch (  Throwable t) {
    observer.onError(t);
  }
  return Subscriptions.empty();
}

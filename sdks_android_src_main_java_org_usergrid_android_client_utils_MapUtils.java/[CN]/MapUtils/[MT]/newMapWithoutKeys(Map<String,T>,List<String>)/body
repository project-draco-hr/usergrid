{
  Map<String,T> newMap=null;
  if (map != null) {
    newMap=new HashMap<String,T>(map);
  }
 else {
    newMap=new HashMap<String,T>();
  }
  for (  String key : keys) {
    newMap.remove(key);
  }
  return newMap;
}

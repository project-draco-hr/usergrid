{
  List<QueueMessage> returnQueue=new ArrayList<>();
  for (int i=0; i < limit; i++) {
    if (!queue.isEmpty()) {
      returnQueue.add(queue.remove(0));
    }
 else {
      break;
    }
  }
  return returnQueue;
}

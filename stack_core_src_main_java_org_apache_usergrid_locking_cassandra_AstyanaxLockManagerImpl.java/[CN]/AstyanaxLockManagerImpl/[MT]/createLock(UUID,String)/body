{
  String lockPath=LockPathBuilder.buildPath(applicationId,path);
  ColumnPrefixDistributedRowLock<String> lock=new ColumnPrefixDistributedRowLock<>(keyspace,columnFamily,lockPath).expireLockAfter(Integer.MAX_VALUE,TimeUnit.MILLISECONDS);
  return new AstyanaxLockImpl(lock);
}

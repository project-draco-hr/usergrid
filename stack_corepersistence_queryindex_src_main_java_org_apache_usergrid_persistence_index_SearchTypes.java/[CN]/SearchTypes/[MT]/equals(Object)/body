{
  if (this == o) {
    return true;
  }
  if (!(o instanceof SearchTypes)) {
    return false;
  }
  final SearchTypes that=(SearchTypes)o;
  if (!Arrays.equals(types,that.types)) {
    return false;
  }
  return true;
}

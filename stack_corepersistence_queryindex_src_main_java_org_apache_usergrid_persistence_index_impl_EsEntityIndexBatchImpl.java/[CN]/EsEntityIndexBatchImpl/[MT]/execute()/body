{
  Observable.from(promises).doOnNext(new Action1<BetterFuture>(){
    @Override public void call(    BetterFuture betterFuture){
      betterFuture.get();
    }
  }
).toBlocking().lastOrDefault(null);
  promises.clear();
}

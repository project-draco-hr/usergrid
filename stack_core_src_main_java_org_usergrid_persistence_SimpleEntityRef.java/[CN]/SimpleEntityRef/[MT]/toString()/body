{
  if ((type == null) && (id == null)) {
    return "EntityRef(" + NULL_ID.toString() + ")";
  }
  if (type == null) {
    return "EntityRef(" + id.toString() + ")";
  }
  return type + "(" + id+ ")";
}

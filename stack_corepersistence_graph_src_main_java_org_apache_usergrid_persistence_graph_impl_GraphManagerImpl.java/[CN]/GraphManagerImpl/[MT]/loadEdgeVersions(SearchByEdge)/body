{
  return HystrixObservable.user(mergedEdgeReader.getEdgeVersions(scope,searchByEdge).buffer(graphFig.getScanPageSize()).flatMap(new EdgeBufferFilter(searchByEdge.getMaxTimestamp())).cast(Edge.class));
}

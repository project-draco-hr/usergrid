{
  return HystrixGraphObservable.user(mergedEdgeReader.getEdgeVersions(scope,searchByEdge).buffer(graphFig.getScanPageSize()).flatMap(new EdgeBufferFilter(searchByEdge.getMaxVersion())).cast(Edge.class));
}

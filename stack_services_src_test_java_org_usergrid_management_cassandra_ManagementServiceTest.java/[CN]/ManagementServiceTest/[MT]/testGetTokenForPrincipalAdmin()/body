{
  String token=managementService.getTokenForPrincipal(ACCESS,null,MANAGEMENT_APPLICATION_ID,AuthPrincipalType.ADMIN_USER,adminUser.getUuid());
  assertNotNull(token);
  token=managementService.getTokenForPrincipal(ACCESS,null,MANAGEMENT_APPLICATION_ID,AuthPrincipalType.APPLICATION_USER,adminUser.getUuid());
  assertNotNull(token);
}

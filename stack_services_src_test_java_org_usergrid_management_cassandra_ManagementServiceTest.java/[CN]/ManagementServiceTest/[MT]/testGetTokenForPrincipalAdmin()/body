{
  String token=managementService.getTokenForPrincipal(MANAGEMENT_APPLICATION_ID,AuthPrincipalType.ADMIN_USER,adminUser.getUuid(),null,true);
  assertNotNull(token);
  token=managementService.getTokenForPrincipal(MANAGEMENT_APPLICATION_ID,AuthPrincipalType.APPLICATION_USER,adminUser.getUuid(),null,true);
  assertNotNull(token);
}

{
  logger.info("Checking filter: " + f);
  assertEquals("Predicate property name not correct",name,f.getPropertyName());
  assertEquals("first predicate operator not correct",op,f.getOperator());
  assertEquals("first predicate value not correct",val,f.getValue());
}

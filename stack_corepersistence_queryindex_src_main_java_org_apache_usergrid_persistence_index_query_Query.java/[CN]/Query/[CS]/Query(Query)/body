{
  if (q == null) {
    return;
  }
  type=q.type;
  sortPredicates=q.sortPredicates != null ? new ArrayList<>(q.sortPredicates) : null;
  startResult=q.startResult;
  cursor=q.cursor;
  limit=q.limit;
  selectAssignments=q.selectAssignments != null ? new LinkedHashMap<>(q.selectAssignments) : null;
  mergeSelectResults=q.mergeSelectResults;
  connectionType=q.connectionType;
  permissions=q.permissions != null ? new ArrayList<>(q.permissions) : null;
  reversed=q.reversed;
  reversedSet=q.reversedSet;
  startTime=q.startTime;
  finishTime=q.finishTime;
  resolution=q.resolution;
  pad=q.pad;
  rootOperand=q.rootOperand;
  identifiers=q.identifiers != null ? new ArrayList<>(q.identifiers) : null;
  counterFilters=q.counterFilters != null ? new ArrayList<>(q.counterFilters) : null;
  collection=q.collection;
  level=q.level;
}

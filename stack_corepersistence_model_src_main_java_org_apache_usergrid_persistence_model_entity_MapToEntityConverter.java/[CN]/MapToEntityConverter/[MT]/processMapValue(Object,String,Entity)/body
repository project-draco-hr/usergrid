{
  if ("location".equals(fieldName.toString().toLowerCase())) {
    processLocationField((Map<String,Object>)value,fieldName,entity);
  }
 else {
    entity.setField(new EntityObjectField(fieldName,fromMap((Map<String,Object>)value,false)));
  }
}

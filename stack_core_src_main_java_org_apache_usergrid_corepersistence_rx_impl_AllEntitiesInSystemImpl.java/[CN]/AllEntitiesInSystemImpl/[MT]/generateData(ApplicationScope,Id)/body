{
  CollectionScope scope=CpNamingUtils.getCollectionScopeNameFromEntityType(applicationScope.getApplication(),nodeId.getType());
  final EntityIdScope idScope=new EntityIdScope(scope,nodeId);
  return idScope;
}

{
  if (credentials == null) {
    return false;
  }
  if (password == null) {
    return false;
  }
  if (credentials.getHashType() != null) {
    password=credentials.encrypt(credentials.getHashType(),"",password);
  }
  return credentials.compareSha1Secret(password);
}

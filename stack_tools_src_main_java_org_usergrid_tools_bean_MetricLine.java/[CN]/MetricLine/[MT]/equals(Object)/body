{
  if (o instanceof MetricLine) {
    MetricLine oth=(MetricLine)o;
    return oth.getMetricSort().equals(metricSort) && oth.getOrgName().equals(orgName) && oth.getAppName().equals(appName)&& oth.getCount() == count;
  }
  return false;
}

{
  String entity_uri=null;
  if (!Application.ENTITY_TYPE.equals(entity.getType())) {
    entity_uri=url_base + application.toString() + "/"+ pluralize(entity.getType())+ "/"+ entity.getUuid();
  }
 else {
    entity_uri=url_base + application.toString();
  }
  return entity_uri;
}

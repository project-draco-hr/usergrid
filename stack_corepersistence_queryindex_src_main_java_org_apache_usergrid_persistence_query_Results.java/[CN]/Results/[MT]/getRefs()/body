{
  if (refs != null) {
    return refs;
  }
  List<?> l=getEntities();
  if ((l != null) && (l.size() > 0)) {
    return (List<EntityRef>)l;
  }
  if (ref != null) {
    refs=new ArrayList<EntityRef>();
    refs.add(ref);
    return refs;
  }
  return new ArrayList<EntityRef>();
}

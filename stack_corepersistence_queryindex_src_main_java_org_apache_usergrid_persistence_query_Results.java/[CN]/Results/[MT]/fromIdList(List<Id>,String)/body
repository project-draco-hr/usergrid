{
  if (type == null) {
    return fromIdList(l);
  }
  List<EntityRef> refs=new ArrayList<EntityRef>();
  for (  Id u : l) {
    refs.add(ref(u));
  }
  Results r=new Results();
  r.setRefs(refs);
  return r;
}

{
  if (entities != null) {
    return entities;
  }
  if (entity != null) {
    entities=new ArrayList<Entity>();
    entities.add(entity);
    return entities;
  }
  return new ArrayList<Entity>();
}

{
  if (ref != null) {
    return ref;
  }
  Entity e=getEntity();
  if (e != null) {
    return ref(e.getId(),e.getVersion());
  }
  Id u=getId();
  if (u != null) {
    return ref(u);
  }
  return null;
}

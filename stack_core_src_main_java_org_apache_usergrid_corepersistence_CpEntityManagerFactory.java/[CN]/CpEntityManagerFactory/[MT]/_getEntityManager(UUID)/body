{
  EntityManager em=new CpEntityManager(cassandraService,counterUtils,indexService,managerCache,metricsFactory,entityManagerFig,graphManagerFactory,collectionService,connectionService,indexProducer,applicationId);
  return em;
}

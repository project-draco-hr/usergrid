{
  EntityManager em=new CpEntityManager(cassandraService,counterUtils,indexService,managerCache,metricsFactory,akkaFig,entityManagerFig,graphManagerFactory,collectionService,connectionService,collectionSettingsCacheFactory,applicationId);
  return em;
}

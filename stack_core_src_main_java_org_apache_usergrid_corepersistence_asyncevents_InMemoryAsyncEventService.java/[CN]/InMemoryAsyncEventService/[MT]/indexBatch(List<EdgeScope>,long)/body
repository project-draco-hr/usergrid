{
  for (  EdgeScope e : edges) {
    final EntityIndexOperation entityIndexOperation=new EntityIndexOperation(e.getApplicationScope(),e.getEdge().getTargetNode(),updatedSince);
    run(eventBuilder.buildEntityIndex(entityIndexOperation));
  }
}

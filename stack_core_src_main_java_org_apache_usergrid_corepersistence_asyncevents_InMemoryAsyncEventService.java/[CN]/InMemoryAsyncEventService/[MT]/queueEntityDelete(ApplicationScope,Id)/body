{
  log.debug("Deleting entity id from index in app scope {} with entityId {} }",applicationScope,entityId);
  final Observable<IndexOperationMessage> edgeObservable=indexService.deleteEntityIndexes(applicationScope,entityId);
  run(edgeObservable);
}

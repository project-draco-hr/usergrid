{
  final EventBuilderImpl.EntityDeleteResults results=eventBuilder.buildEntityDelete(applicationScope,entityId);
  run(results.getIndexObservable());
  run(results.getEntitiesDeleted());
  run(results.getCompactedNode());
}

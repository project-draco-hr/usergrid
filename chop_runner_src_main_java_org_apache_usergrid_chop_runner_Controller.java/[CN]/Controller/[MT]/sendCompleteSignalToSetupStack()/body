{
  LOG.info("Sending complete signal to Coordinator");
  WebResource resource=Client.create().resource(coordinatorFig.getEndpoint());
  LOG.info("USERNAME: {}",coordinatorFig.getUsername());
  LOG.info("MODULE_GROUPID: {}",project.getGroupId());
  LOG.info("MODULE_ARTIFACTID: {}",project.getArtifactId());
  LOG.info("MODULE_VERSION: {}",project.getVersion());
  LOG.info("COMMIT_ID: {}",project.getVcsVersion());
  SetupStackState result=resource.path(coordinatorFig.getRunnersCompletedPath()).queryParam(USERNAME,coordinatorFig.getUsername()).queryParam(MODULE_GROUPID,project.getGroupId()).queryParam(MODULE_ARTIFACTID,project.getArtifactId()).queryParam(MODULE_VERSION,project.getVersion()).queryParam(COMMIT_ID,project.getVcsVersion()).type(MediaType.APPLICATION_JSON).get(SetupStackState.class);
  return result;
}

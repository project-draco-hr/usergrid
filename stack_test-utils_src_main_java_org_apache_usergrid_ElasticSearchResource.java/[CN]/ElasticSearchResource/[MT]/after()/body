{
  log.info("Entering after");
synchronized (mutex) {
    if (process != null) {
      log.info("----------------------------------------------------------------------");
      log.info("Destroying ElasticSearch running on port " + port);
      log.info("----------------------------------------------------------------------");
      process.destroy();
      started=false;
    }
  }
  log.info("Leaving after");
}

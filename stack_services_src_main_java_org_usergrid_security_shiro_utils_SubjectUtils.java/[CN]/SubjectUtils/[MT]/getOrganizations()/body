{
  Subject currentUser=getSubject();
  if (!isOrganizationAdmin()) {
    return null;
  }
  Session session=currentUser.getSession();
  @SuppressWarnings("unchecked") BiMap<UUID,String> organizations=(BiMap<UUID,String>)session.getAttribute("organizations");
  return organizations;
}

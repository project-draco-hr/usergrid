{
  if ((type == null) && (uuid == null)) {
    return "EntityRef(" + NULL_ID.toString() + ")";
  }
  if (type == null) {
    return "EntityRef(" + uuid.toString() + ")";
  }
  return type + "(" + uuid+ ")";
}

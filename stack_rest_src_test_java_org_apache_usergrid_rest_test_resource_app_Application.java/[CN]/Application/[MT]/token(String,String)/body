{
  String url=String.format("%s/token",url());
  JsonNode node=resource().path(url).queryParam("grant_type","password").queryParam("username",username).queryParam("password",password).accept(MediaType.APPLICATION_JSON).type(MediaType.APPLICATION_JSON_TYPE).get(JsonNode.class);
  return node.get("access_token").asText();
}

{
synchronized (this) {
    if (worker != null) {
      return;
    }
    worker=Schedulers.newThread().createWorker();
    worker.schedulePeriodically(new TimeoutTask<T>(this,consistencyFig),consistencyFig.getTaskLoopTime(),consistencyFig.getTaskLoopTime(),TimeUnit.MILLISECONDS);
  }
}
